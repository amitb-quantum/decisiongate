# QuantumEagle scope regression

This neutral fixture represents the motivating failure without naming a real program or fabricating solicitation facts.

Run it from the repository root:

```bash
decisiongate evaluate --case cases/quantumeagle_scope/case.json --output build/quantumeagle
```

Expected disposition: `HUMAN_VERIFY`.

The apparent fit is supported only by model interpretation and an applicant assumption. The decision-changing question asks the responsible authority whether the topic is eligible as an independent primary objective. The later Program Manager outcome is held out and not used as pre-decision evidence.

